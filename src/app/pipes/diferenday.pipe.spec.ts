import { DiferendayPipe } from './diferenday.pipe';

describe('DiferendayPipe', () => {
  it('create an instance', () => {
    const pipe = new DiferendayPipe();
    expect(pipe).toBeTruthy();
  });
});
