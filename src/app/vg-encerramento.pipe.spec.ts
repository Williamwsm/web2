import { VgEncerramentoPipe } from './vg-encerramento.pipe';

describe('VgEncerramentoPipe', () => {
  it('create an instance', () => {
    const pipe = new VgEncerramentoPipe();
    expect(pipe).toBeTruthy();
  });
});
